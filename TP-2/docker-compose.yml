services:
  api:
    image: zouzouprod291/notes-api:v1
    ports:
      - "3000:3000"
    env_file:
      - .env

  db:
    image: postgres:15
    env_file:
      - .env
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
